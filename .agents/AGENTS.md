# Project Agent Rules

- **Deploy Always**: Whenever making changes to the application code, ALWAYS run the deployment script (`npm run deploy` or `./deploy.sh`) to deploy the changes to production immediately. Do not wait for the user to ask you to deploy.
